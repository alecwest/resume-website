import { ViewFriendlyPipe } from './view-friendly.pipe';

describe('ViewFriendlyPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewFriendlyPipe();
    expect(pipe).toBeTruthy();
  });
});
